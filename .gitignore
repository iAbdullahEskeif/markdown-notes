# Node modules directory
/node_modules/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-specific files
.DS_Store          # macOS
Thumbs.db          # Windows

# Environment variables
.env               # Store sensitive data like API keys, database credentials

# npm package lock files (optional but recommended to be ignored in some cases)
package-lock.json  # If you don't want to commit the package-lock file
yarn.lock          # If you're using Yarn and prefer not to commit this file

# Build directories (for compiled files, bundlers, etc.)
dist/
build/
tmp/
.cache/

# Coverage reports (generated by testing tools like Jest, Moch

